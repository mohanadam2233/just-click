"use client";
import ErrorMain from "@/components/layout/main/ErrorMain";
import React from "react";

const erroError = ({ error, reset }) => {
  return (
    <main>
      <ErrorMain />
    </main>
  );
};

export default erroError;
